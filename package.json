{
  "name": "api-health-check-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for checking API health status by calling GET localhost:8080/api/ticket",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node build/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["mcp", "server", "api", "health", "check"],
  "author": "",
  "license": "MIT"
}